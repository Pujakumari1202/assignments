// this is called file based routing (we dont need to render it on page.tsx file we only have to create folder inside only need to create function no need to render it)
export default function Home(){
    return (
        <div>
            Users page
        </div>
    );
}